# Optional: enables the "AI Illustrated Portraits" feature on the Characters tab.
# Without this set, the app still works fully; portrait generation just returns
# a friendly error and the app falls back to initials.
# Get a key from https://console.anthropic.com/
ANTHROPIC_API_KEY=
